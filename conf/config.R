config <- list(
  common = list(
    use_cols = NULL,
    ignore_cols = NULL,
    phe_as_fam = FALSE,
    remove_dups = TRUE,
    maf_thresh = 0.01
  ),
  lasso = list(
    signif_level = 0.001,
    alpha = 0.7
  )
)
