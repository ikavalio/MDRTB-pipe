---
output:
  html_document:
    toc: yes
---

```{r include=FALSE, echo=FALSE, message=FALSE, warning=FALSE, error=FALSE}
library(ggplot2)
library(knitr)
library(glmnet)
library(boot)
library(qqman)

scriptdir <- "D:\\work\\bio\\rlib\\common"
source(file.path(scriptdir, "config.R"))

source(lib.reader)
source(lib.summary)

### RMD PARAMS
out.base <- home.lasso
sign.bound <- 0.001
### RMD PARAMS


out <- NULL
for(dataset.dir in raw.files) {
  out <- c(out, knit_child("linear-models-dataset.Rmd"))
}
```

`r paste(out, collapse='\n')`